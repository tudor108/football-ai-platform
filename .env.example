# Local development only. Never commit the real .env file.
API_FOOTBALL_KEY=
GCS_BUCKET=
GCP_PROJECT_ID=

# Optional pipeline/Agent Search settings
AGENT_SEARCH_DATA_STORE_ID=
AGENT_SEARCH_LOCATION=global

# Agent runtime settings (used by football-cluster-agent)
GOOGLE_CLOUD_PROJECT=
CLUSTER_BUCKET=
BQ_DATASET=football_analytics

# Optional cost-guard service settings
PROJECT_ID=
TARGET_REGION=europe-central2
TARGET_SERVICE=football-agent
